PRIVATE_KEY=
ETHERSCAN_API_KEY=

# Network (already configured)
BASE_SEPOLIA_RPC_URL=
BASE_SEPOLIA_PRIVATE_KEY=

# Test-specific
OWNER_ADDRESS=                # Owner of AssuraVerifier
USER_PRIVATE_KEY=               # Optional: User account (defaults to deployer)

# TEE Service (fetches TEE address and attestations)
TEE_SERVICE_URL=https://tee.assura.network  # TEE service endpoint (production) or http://localhost:3000 (local dev)